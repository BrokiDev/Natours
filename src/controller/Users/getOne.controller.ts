import { Props } from "../../types";

export const getOneUser = ({ req, res }: Props) => {
    res.status(500).json({
      status: "error",
      message: "This route is not defined yet",
    });
  };